/**
 * SQL Operations Demo for SynapCores Node.js SDK
 *
 * This example focuses specifically on SQL operations:
 * - Table management (CREATE, ALTER, DROP, DESCRIBE, SHOW)
 * - Index management (CREATE INDEX, DROP INDEX, SHOW INDEXES)
 * - Transaction support (BEGIN, COMMIT, ROLLBACK)
 * - Batch operations (INSERT, UPDATE, DELETE)
 * - Advanced features (CTEs, window functions, JSON operations, prepared statements)
 */

import { SynapCores } from '../src/client';
import {
  ColumnDefinition,
  CreateTableOptions,
  AlterTableOptions,
  IndexDefinition,
  TransactionOptions,
  BatchInsertOptions,
  BatchUpdateOptions,
  CTEDefinition,
  WindowFunctionOptions,
} from '../src/types/client';

async function tableManagementDemo(client: SynapCores) {
  console.log('📊 TABLE MANAGEMENT OPERATIONS');
  console.log('==============================');

  // Define comprehensive table schema
  const employeeColumns: ColumnDefinition[] = [
    {
      name: 'id',
      dataType: 'INTEGER',
      constraints: [{ type: 'PRIMARY_KEY' }]
    },
    {
      name: 'employee_number',
      dataType: 'VARCHAR(20)',
      constraints: [{ type: 'UNIQUE' }, { type: 'NOT_NULL' }]
    },
    {
      name: 'first_name',
      dataType: 'VARCHAR(50)',
      constraints: [{ type: 'NOT_NULL' }]
    },
    {
      name: 'last_name',
      dataType: 'VARCHAR(50)',
      constraints: [{ type: 'NOT_NULL' }]
    },
    {
      name: 'email',
      dataType: 'VARCHAR(255)',
      constraints: [
        { type: 'UNIQUE' },
        { type: 'NOT_NULL' },
        { type: 'CHECK', expression: "email LIKE '%@%.%'" }
      ]
    },
    {
      name: 'department_id',
      dataType: 'INTEGER'
    },
    {
      name: 'salary',
      dataType: 'DECIMAL(10,2)',
      constraints: [{ type: 'CHECK', expression: 'salary > 0' }]
    },
    {
      name: 'hire_date',
      dataType: 'DATE',
      constraints: [{ type: 'NOT_NULL' }]
    },
    {
      name: 'profile_data',
      dataType: 'JSONB'
    },
    {
      name: 'is_active',
      dataType: 'BOOLEAN',
      defaultValue: true
    },
    {
      name: 'created_at',
      dataType: 'TIMESTAMP',
      defaultValue: 'CURRENT_TIMESTAMP'
    },
    {
      name: 'updated_at',
      dataType: 'TIMESTAMP',
      defaultValue: 'CURRENT_TIMESTAMP'
    }
  ];\n\n  const tableOptions: CreateTableOptions = {\n    ifNotExists: true,\n    constraints: [\n      {\n        type: 'CHECK',\n        columns: ['hire_date'],\n        expression: 'hire_date <= CURRENT_DATE'\n      }\n    ]\n  };\n\n  // Create employees table\n  console.log('🏗️  Creating employees table...');\n  await client.createTable('employees', employeeColumns, tableOptions);\n  console.log('✅ Employees table created');\n\n  // Create departments table for foreign key demonstration\n  const departmentColumns: ColumnDefinition[] = [\n    {\n      name: 'id',\n      dataType: 'INTEGER',\n      constraints: [{ type: 'PRIMARY_KEY' }]\n    },\n    {\n      name: 'name',\n      dataType: 'VARCHAR(100)',\n      constraints: [{ type: 'UNIQUE' }, { type: 'NOT_NULL' }]\n    },\n    {\n      name: 'budget',\n      dataType: 'DECIMAL(12,2)'\n    },\n    {\n      name: 'manager_id',\n      dataType: 'INTEGER'\n    }\n  ];\n\n  console.log('🏗️  Creating departments table...');\n  await client.createTable('departments', departmentColumns, { ifNotExists: true });\n  console.log('✅ Departments table created');\n\n  // Show all tables\n  console.log('\\n📋 Listing all tables:');\n  const tables = await client.showTables();\n  console.log(tables);\n\n  // Describe table structure\n  console.log('\\n📖 Describing employees table:');\n  try {\n    const tableInfo = await client.describeTable('employees');\n    console.log('Table structure:');\n    tableInfo.columns.forEach(col => {\n      console.log(`  ${col.name}: ${col.type} ${col.nullable ? '' : 'NOT NULL'} ${col.isPrimaryKey ? 'PRIMARY KEY' : ''}`);\n    });\n  } catch (error) {\n    console.log('⚠️  Table description not available via REST API, using SQL fallback');\n    const result = await client.sql('SELECT * FROM information_schema.columns WHERE table_name = \\'employees\\'');\n    console.log(`Found ${result.rowCount} columns`);\n  }\n\n  // Alter table - add new column\n  console.log('\\n🔧 Altering table - adding phone column...');\n  const alterOptions: AlterTableOptions = {\n    action: 'ADD_COLUMN',\n    columnDefinition: {\n      name: 'phone',\n      dataType: 'VARCHAR(20)'\n    }\n  };\n\n  try {\n    await client.alterTable('employees', alterOptions);\n    console.log('✅ Phone column added');\n  } catch (error) {\n    console.log('⚠️  ALTER TABLE not supported, using SQL fallback');\n    await client.sql('ALTER TABLE employees ADD COLUMN phone VARCHAR(20)');\n    console.log('✅ Phone column added via SQL');\n  }\n\n  // Add foreign key constraint (if supported)\n  console.log('\\n🔗 Adding foreign key constraint...');\n  try {\n    await client.sql('ALTER TABLE employees ADD CONSTRAINT fk_department FOREIGN KEY (department_id) REFERENCES departments(id)');\n    console.log('✅ Foreign key constraint added');\n  } catch (error) {\n    console.log('⚠️  Foreign key constraint not added (may not be supported)');\n  }\n}\n\nasync function indexManagementDemo(client: SynapCores) {\n  console.log('\\n🔍 INDEX MANAGEMENT');\n  console.log('===================');\n\n  // Create single-column index\n  const emailIndex: IndexDefinition = {\n    name: 'idx_employees_email',\n    tableName: 'employees',\n    columns: [{ name: 'email', order: 'ASC' }],\n    unique: true,\n    ifNotExists: true\n  };\n\n  console.log('📇 Creating unique email index...');\n  await client.createIndex(emailIndex);\n  console.log('✅ Email index created');\n\n  // Create composite index\n  const nameIndex: IndexDefinition = {\n    name: 'idx_employees_name',\n    tableName: 'employees',\n    columns: [\n      { name: 'last_name', order: 'ASC' },\n      { name: 'first_name', order: 'ASC' }\n    ],\n    ifNotExists: true\n  };\n\n  console.log('📇 Creating composite name index...');\n  await client.createIndex(nameIndex);\n  console.log('✅ Composite name index created');\n\n  // Create performance index\n  const performanceIndex: IndexDefinition = {\n    name: 'idx_employees_dept_salary',\n    tableName: 'employees',\n    columns: [\n      { name: 'department_id', order: 'ASC' },\n      { name: 'salary', order: 'DESC' }\n    ],\n    ifNotExists: true\n  };\n\n  console.log('📇 Creating performance index...');\n  await client.createIndex(performanceIndex);\n  console.log('✅ Performance index created');\n\n  // Show all indexes\n  console.log('\\n📋 Listing all indexes:');\n  try {\n    const indexes = await client.showIndexes();\n    console.log('Available indexes:');\n    indexes.forEach(idx => {\n      console.log(`  ${idx.name} on ${idx.table} (${idx.columns.join(', ')}) ${idx.unique ? 'UNIQUE' : ''}`);\n    });\n  } catch (error) {\n    console.log('⚠️  Index listing not available via REST API, using SQL fallback');\n    const result = await client.sql('SELECT name FROM sqlite_master WHERE type = \\'index\\'');\n    console.log(`Found ${result.rowCount} indexes`);\n  }\n}\n\nasync function transactionDemo(client: SynapCores) {\n  console.log('\\n💳 TRANSACTION MANAGEMENT');\n  console.log('=========================');\n\n  // Insert sample departments first\n  console.log('📝 Inserting sample departments...');\n  await client.sql(`\n    INSERT OR REPLACE INTO departments (id, name, budget, manager_id) VALUES\n    (1, 'Engineering', 1000000.00, NULL),\n    (2, 'Sales', 500000.00, NULL),\n    (3, 'Marketing', 300000.00, NULL),\n    (4, 'HR', 200000.00, NULL)\n  `);\n  console.log('✅ Sample departments inserted');\n\n  // Transaction example 1: Successful transaction\n  console.log('\\n🔄 Transaction Example 1: Successful Transaction');\n  const transactionOptions: TransactionOptions = {\n    isolationLevel: 'READ_COMMITTED',\n    readOnly: false,\n    timeout: 10000\n  };\n\n  const transaction1 = await client.beginTransaction(transactionOptions);\n  console.log(`✅ Transaction ${transaction1.id} started`);\n\n  try {\n    // Insert multiple employees in a transaction\n    await client.sql(`\n      INSERT INTO employees (id, employee_number, first_name, last_name, email, department_id, salary, hire_date, profile_data)\n      VALUES (1, 'EMP001', 'Alice', 'Johnson', 'alice.johnson@company.com', 1, 95000.00, '2024-01-15', '{\"skills\": [\"Python\", \"JavaScript\"], \"level\": \"senior\"}')\n    `);\n\n    await client.sql(`\n      INSERT INTO employees (id, employee_number, first_name, last_name, email, department_id, salary, hire_date, profile_data)\n      VALUES (2, 'EMP002', 'Bob', 'Smith', 'bob.smith@company.com', 1, 87000.00, '2024-02-01', '{\"skills\": [\"Java\", \"React\"], \"level\": \"mid\"}')\n    `);\n\n    await client.sql(`\n      INSERT INTO employees (id, employee_number, first_name, last_name, email, department_id, salary, hire_date, profile_data)\n      VALUES (3, 'EMP003', 'Carol', 'Davis', 'carol.davis@company.com', 2, 75000.00, '2024-01-20', '{\"skills\": [\"Sales\", \"CRM\"], \"level\": \"senior\"}')\n    `);\n\n    console.log('✅ Employees inserted successfully');\n    await client.commitTransaction();\n    console.log('✅ Transaction committed successfully');\n\n  } catch (error) {\n    console.log('❌ Error in transaction, rolling back...');\n    await client.rollbackTransaction();\n    throw error;\n  }\n\n  // Transaction example 2: Rollback demonstration\n  console.log('\\n🔄 Transaction Example 2: Rollback Demonstration');\n  const transaction2 = await client.beginTransaction();\n  console.log(`✅ Transaction ${transaction2.id} started`);\n\n  try {\n    // Insert a valid employee\n    await client.sql(`\n      INSERT INTO employees (id, employee_number, first_name, last_name, email, department_id, salary, hire_date)\n      VALUES (100, 'EMP100', 'Test', 'Employee', 'test@company.com', 1, 50000.00, '2024-03-01')\n    `);\n\n    // Try to insert invalid data (this should fail)\n    await client.sql(`\n      INSERT INTO employees (id, employee_number, first_name, last_name, email, department_id, salary, hire_date)\n      VALUES (101, 'EMP100', 'Duplicate', 'Employee', 'duplicate@company.com', 1, -1000.00, '2024-03-01')\n    `);\n\n    await client.commitTransaction();\n\n  } catch (error) {\n    console.log('❌ Expected error occurred, rolling back transaction...');\n    await client.rollbackTransaction();\n    console.log('✅ Transaction rolled back successfully');\n\n    // Verify rollback worked\n    const result = await client.sql('SELECT COUNT(*) as count FROM employees WHERE employee_number = \\'EMP100\\'');\n    console.log(`Employees with EMP100: ${result.rows[0].count} (should be 0)`);\n  }\n}\n\nasync function batchOperationsDemo(client: SynapCores) {\n  console.log('\\n📦 BATCH OPERATIONS');\n  console.log('===================');\n\n  // Batch insert employees\n  console.log('📥 Batch Insert Example:');\n  const employeeData = [\n    {\n      id: 10,\n      employee_number: 'EMP010',\n      first_name: 'David',\n      last_name: 'Wilson',\n      email: 'david.wilson@company.com',\n      department_id: 2,\n      salary: 82000.00,\n      hire_date: '2024-02-15',\n      profile_data: JSON.stringify({ skills: ['Sales', 'Negotiation'], level: 'senior' })\n    },\n    {\n      id: 11,\n      employee_number: 'EMP011',\n      first_name: 'Emma',\n      last_name: 'Brown',\n      email: 'emma.brown@company.com',\n      department_id: 3,\n      salary: 70000.00,\n      hire_date: '2024-01-30',\n      profile_data: JSON.stringify({ skills: ['Marketing', 'Analytics'], level: 'mid' })\n    },\n    {\n      id: 12,\n      employee_number: 'EMP012',\n      first_name: 'Frank',\n      last_name: 'Miller',\n      email: 'frank.miller@company.com',\n      department_id: 4,\n      salary: 65000.00,\n      hire_date: '2024-02-10',\n      profile_data: JSON.stringify({ skills: ['HR', 'Recruitment'], level: 'mid' })\n    }\n  ];\n\n  const batchInsertOptions: BatchInsertOptions = {\n    tableName: 'employees',\n    columns: ['id', 'employee_number', 'first_name', 'last_name', 'email', 'department_id', 'salary', 'hire_date', 'profile_data'],\n    rows: employeeData,\n    onConflict: 'REPLACE',\n    batchSize: 100\n  };\n\n  try {\n    const batchResult = await client.batchInsert(batchInsertOptions);\n    console.log(`✅ Batch insert completed: ${batchResult.successful}/${batchResult.totalProcessed} successful`);\n    if (batchResult.errors && batchResult.errors.length > 0) {\n      console.log('Errors:', batchResult.errors);\n    }\n  } catch (error) {\n    console.log('⚠️  Batch insert not available via REST API, using SQL fallback');\n    for (const emp of employeeData) {\n      await client.sql(`\n        INSERT OR REPLACE INTO employees (id, employee_number, first_name, last_name, email, department_id, salary, hire_date, profile_data)\n        VALUES ($1, $2, $3, $4, $5, $6, $7, $8, $9)\n      `, {\n        1: emp.id, 2: emp.employee_number, 3: emp.first_name, 4: emp.last_name,\n        5: emp.email, 6: emp.department_id, 7: emp.salary, 8: emp.hire_date, 9: emp.profile_data\n      });\n    }\n    console.log('✅ Employees inserted via SQL fallback');\n  }\n\n  // Batch update example\n  console.log('\\n📝 Batch Update Example:');\n  const updateData = [\n    { set: { salary: 98000.00 }, where: { employee_number: 'EMP001' } },\n    { set: { salary: 90000.00 }, where: { employee_number: 'EMP002' } },\n    { set: { salary: 78000.00 }, where: { employee_number: 'EMP003' } }\n  ];\n\n  const batchUpdateOptions: BatchUpdateOptions = {\n    tableName: 'employees',\n    updates: updateData,\n    batchSize: 50\n  };\n\n  try {\n    const updateResult = await client.batchUpdate(batchUpdateOptions);\n    console.log(`✅ Batch update completed: ${updateResult.successful}/${updateResult.totalProcessed} successful`);\n  } catch (error) {\n    console.log('⚠️  Batch update not available via REST API, using SQL fallback');\n    for (const update of updateData) {\n      await client.sql(`\n        UPDATE employees SET salary = $1 WHERE employee_number = $2\n      `, { 1: update.set.salary, 2: update.where.employee_number });\n    }\n    console.log('✅ Salaries updated via SQL fallback');\n  }\n}\n\nasync function advancedSQLDemo(client: SynapCores) {\n  console.log('\\n⚡ ADVANCED SQL FEATURES');\n  console.log('========================');\n\n  // Prepared Statements\n  console.log('📝 Prepared Statements:');\n  try {\n    const preparedStmt = await client.prepareStatement(\n      'SELECT * FROM employees WHERE department_id = $1 AND salary > $2',\n      { name: 'find_high_earners', parameterTypes: ['INTEGER', 'DECIMAL'] }\n    );\n    console.log(`✅ Prepared statement created: ${preparedStmt.id}`);\n\n    const result1 = await client.executePrepared(preparedStmt.id, [1, 80000]);\n    console.log(`📊 Found ${result1.rowCount} high earners in Engineering`);\n\n    const result2 = await client.executePrepared('find_high_earners', [2, 70000]);\n    console.log(`📊 Found ${result2.rowCount} high earners in Sales`);\n\n    await client.deallocatePrepared('find_high_earners');\n    console.log('✅ Prepared statement deallocated');\n\n  } catch (error) {\n    console.log('⚠️  Prepared statements not available, using direct SQL');\n    const result = await client.sql('SELECT * FROM employees WHERE department_id = 1 AND salary > 80000');\n    console.log(`📊 Found ${result.rowCount} high earners in Engineering`);\n  }\n\n  // Common Table Expressions (CTEs)\n  console.log('\\n🔗 Common Table Expressions (CTEs):');\n  const ctes: CTEDefinition[] = [\n    {\n      name: 'department_stats',\n      query: `\n        SELECT\n          d.id,\n          d.name,\n          COUNT(e.id) as employee_count,\n          AVG(e.salary) as avg_salary,\n          MAX(e.salary) as max_salary\n        FROM departments d\n        LEFT JOIN employees e ON d.id = e.department_id\n        GROUP BY d.id, d.name\n      `\n    },\n    {\n      name: 'high_performers',\n      query: `\n        SELECT e.*\n        FROM employees e\n        JOIN department_stats ds ON e.department_id = ds.id\n        WHERE e.salary > ds.avg_salary * 1.1\n      `\n    }\n  ];\n\n  try {\n    const cteResult = await client.queryWithCTEs(\n      ctes,\n      `\n        SELECT\n          ds.name as department,\n          ds.employee_count,\n          ds.avg_salary,\n          COUNT(hp.id) as high_performers\n        FROM department_stats ds\n        LEFT JOIN high_performers hp ON ds.id = hp.department_id\n        GROUP BY ds.id, ds.name, ds.employee_count, ds.avg_salary\n        ORDER BY ds.name\n      `\n    );\n\n    console.log('📊 Department analysis with CTEs:');\n    cteResult.rows.forEach(row => {\n      console.log(`  ${row.department}: ${row.employee_count} employees, avg salary: $${row.avg_salary?.toFixed(2)}, high performers: ${row.high_performers}`);\n    });\n\n  } catch (error) {\n    console.log('⚠️  CTEs not supported, using subquery fallback');\n    const fallbackResult = await client.sql(`\n      SELECT\n        d.name as department,\n        COUNT(e.id) as employee_count,\n        AVG(e.salary) as avg_salary\n      FROM departments d\n      LEFT JOIN employees e ON d.id = e.department_id\n      GROUP BY d.id, d.name\n      ORDER BY d.name\n    `);\n\n    console.log('📊 Department statistics (fallback):');\n    fallbackResult.rows.forEach(row => {\n      console.log(`  ${row.department}: ${row.employee_count} employees, avg salary: $${row.avg_salary?.toFixed(2)}`);\n    });\n  }\n\n  // Window Functions\n  console.log('\\n🪟 Window Functions:');\n  const windowFunctions = [\n    {\n      alias: 'salary_rank',\n      function: 'ROW_NUMBER()',\n      options: {\n        partitionBy: ['department_id'],\n        orderBy: [{ column: 'salary', direction: 'DESC' as const }]\n      }\n    },\n    {\n      alias: 'running_total',\n      function: 'SUM(salary)',\n      options: {\n        partitionBy: ['department_id'],\n        orderBy: [{ column: 'hire_date', direction: 'ASC' as const }],\n        frame: { type: 'ROWS' as const, start: 'UNBOUNDED PRECEDING' }\n      }\n    }\n  ];\n\n  try {\n    const windowResult = await client.queryWithWindowFunctions(\n      `\n        SELECT\n          e.first_name,\n          e.last_name,\n          d.name as department,\n          e.salary,\n          e.hire_date\n        FROM employees e\n        JOIN departments d ON e.department_id = d.id\n      `,\n      windowFunctions\n    );\n\n    console.log('📊 Employee ranking with window functions:');\n    windowResult.rows.slice(0, 5).forEach(row => {\n      console.log(`  ${row.first_name} ${row.last_name} (${row.department}): Rank ${row.salary_rank}, Running total: $${row.running_total?.toFixed(2)}`);\n    });\n\n  } catch (error) {\n    console.log('⚠️  Window functions not supported, using standard query');\n    const fallbackResult = await client.sql(`\n      SELECT\n        e.first_name,\n        e.last_name,\n        d.name as department,\n        e.salary\n      FROM employees e\n      JOIN departments d ON e.department_id = d.id\n      ORDER BY e.salary DESC\n      LIMIT 5\n    `);\n\n    console.log('📊 Top 5 earners (fallback):');\n    fallbackResult.rows.forEach((row, i) => {\n      console.log(`  ${i + 1}. ${row.first_name} ${row.last_name} (${row.department}): $${row.salary}`);\n    });\n  }\n\n  // JSON Operations\n  console.log('\\n📄 JSON Operations:');\n  try {\n    // Extract JSON data\n    const jsonResult = await client.jsonQuery(\n      'employees',\n      'profile_data',\n      'extract',\n      'skills'\n    );\n    console.log('📊 Employee skills extracted from JSON:');\n    jsonResult.rows.slice(0, 3).forEach(row => {\n      console.log(`  Skills: ${row.extracted_value}`);\n    });\n\n    // Update JSON data\n    await client.jsonQuery(\n      'employees',\n      'profile_data',\n      'update',\n      'last_review',\n      new Date().toISOString(),\n      \"employee_number = 'EMP001'\"\n    );\n    console.log('✅ JSON data updated for EMP001');\n\n    // Query with JSON contains\n    const containsResult = await client.jsonQuery(\n      'employees',\n      'profile_data',\n      'contains',\n      'level',\n      'senior'\n    );\n    console.log(`📊 Found ${containsResult.rowCount} senior employees`);\n\n  } catch (error) {\n    console.log('⚠️  JSON operations not fully supported, using SQL fallback');\n    try {\n      const fallbackResult = await client.sql(`\n        SELECT first_name, last_name, profile_data\n        FROM employees\n        WHERE profile_data IS NOT NULL\n        LIMIT 3\n      `);\n      console.log('📊 Employee profiles (fallback):');\n      fallbackResult.rows.forEach(row => {\n        console.log(`  ${row.first_name} ${row.last_name}: ${row.profile_data}`);\n      });\n    } catch (sqlError) {\n      console.log('⚠️  JSON queries not supported in this SQL dialect');\n    }\n  }\n}\n\nasync function analyticsAndReportingDemo(client: SynapCores) {\n  console.log('\\n📈 ANALYTICS AND REPORTING');\n  console.log('===========================');\n\n  // Department overview\n  console.log('🏢 Department Overview:');\n  const deptResult = await client.sql(`\n    SELECT\n      d.name as department,\n      COUNT(e.id) as total_employees,\n      AVG(e.salary) as avg_salary,\n      MIN(e.salary) as min_salary,\n      MAX(e.salary) as max_salary,\n      SUM(e.salary) as total_payroll\n    FROM departments d\n    LEFT JOIN employees e ON d.id = e.department_id\n    GROUP BY d.id, d.name\n    ORDER BY total_payroll DESC\n  `);\n\n  deptResult.rows.forEach(row => {\n    console.log(`  ${row.department}:`);\n    console.log(`    Employees: ${row.total_employees}`);\n    console.log(`    Avg Salary: $${row.avg_salary?.toFixed(2)}`);\n    console.log(`    Salary Range: $${row.min_salary} - $${row.max_salary}`);\n    console.log(`    Total Payroll: $${row.total_payroll?.toFixed(2)}`);\n    console.log();\n  });\n\n  // Salary distribution\n  console.log('💰 Salary Distribution:');\n  const salaryResult = await client.sql(`\n    SELECT\n      CASE\n        WHEN salary < 60000 THEN 'Under $60K'\n        WHEN salary < 80000 THEN '$60K - $80K'\n        WHEN salary < 100000 THEN '$80K - $100K'\n        ELSE 'Over $100K'\n      END as salary_range,\n      COUNT(*) as employee_count,\n      AVG(salary) as avg_in_range\n    FROM employees\n    GROUP BY\n      CASE\n        WHEN salary < 60000 THEN 'Under $60K'\n        WHEN salary < 80000 THEN '$60K - $80K'\n        WHEN salary < 100000 THEN '$80K - $100K'\n        ELSE 'Over $100K'\n      END\n    ORDER BY MIN(salary)\n  `);\n\n  salaryResult.rows.forEach(row => {\n    console.log(`  ${row.salary_range}: ${row.employee_count} employees (avg: $${row.avg_in_range?.toFixed(2)})`);\n  });\n\n  // Recent hires\n  console.log('\\n👥 Recent Hires (Last 90 days):');\n  const recentHiresResult = await client.sql(`\n    SELECT\n      e.first_name,\n      e.last_name,\n      e.email,\n      d.name as department,\n      e.salary,\n      e.hire_date\n    FROM employees e\n    JOIN departments d ON e.department_id = d.id\n    WHERE e.hire_date >= DATE('now', '-90 days')\n    ORDER BY e.hire_date DESC\n  `);\n\n  if (recentHiresResult.rowCount > 0) {\n    recentHiresResult.rows.forEach(row => {\n      console.log(`  ${row.first_name} ${row.last_name} (${row.department}) - Hired: ${row.hire_date}`);\n    });\n  } else {\n    console.log('  No recent hires in the last 90 days');\n  }\n\n  // Performance metrics\n  console.log('\\n📊 Performance Metrics:');\n  const metricsResult = await client.sql(`\n    SELECT\n      COUNT(*) as total_employees,\n      COUNT(DISTINCT department_id) as departments_with_staff,\n      AVG(salary) as company_avg_salary,\n      (SELECT COUNT(*) FROM departments) as total_departments,\n      SUM(salary) as total_payroll\n    FROM employees\n  `);\n\n  const metrics = metricsResult.rows[0];\n  console.log(`  Total Employees: ${metrics.total_employees}`);\n  console.log(`  Active Departments: ${metrics.departments_with_staff}/${metrics.total_departments}`);\n  console.log(`  Company Average Salary: $${metrics.company_avg_salary?.toFixed(2)}`);\n  console.log(`  Total Payroll: $${metrics.total_payroll?.toFixed(2)}`);\n}\n\nasync function mainSQLDemo() {\n  const client = new SynapCores({\n    host: 'localhost',\n    port: 8080,\n    apiKey: 'aidb_your_api_key_here', // Replace with your actual API key\n    useHttps: false,\n    timeout: 30000,\n  });\n\n  console.log('🚀 SQL Operations Demo Starting\\n');\n\n  try {\n    await tableManagementDemo(client);\n    await indexManagementDemo(client);\n    await transactionDemo(client);\n    await batchOperationsDemo(client);\n    await advancedSQLDemo(client);\n    await analyticsAndReportingDemo(client);\n\n    console.log('\\n🎉 SQL Operations Demo Completed Successfully!');\n    console.log('============================================');\n\n  } catch (error) {\n    console.error('❌ SQL demo failed:', error);\n\n    if (error instanceof Error) {\n      console.error('Error details:', {\n        name: error.name,\n        message: error.message,\n        stack: error.stack?.split('\\n').slice(0, 5).join('\\n') // First 5 lines of stack\n      });\n    }\n\n    // Cleanup on error\n    try {\n      if (client.getCurrentTransaction()) {\n        await client.rollbackTransaction();\n        console.log('🔄 Transaction rolled back due to error');\n      }\n    } catch (rollbackError) {\n      console.error('❌ Failed to rollback transaction:', rollbackError);\n    }\n  }\n}\n\n// Cleanup function\nasync function cleanupDemo(client: SynapCores) {\n  console.log('\\n🧹 CLEANUP OPERATIONS');\n  console.log('=====================');\n\n  try {\n    // Drop indexes\n    const indexesToDrop = [\n      'idx_employees_email',\n      'idx_employees_name',\n      'idx_employees_dept_salary'\n    ];\n\n    for (const indexName of indexesToDrop) {\n      try {\n        await client.dropIndex(indexName, { ifExists: true });\n        console.log(`✅ Dropped index: ${indexName}`);\n      } catch (error) {\n        console.log(`⚠️  Could not drop index ${indexName}`);\n      }\n    }\n\n    // Optionally drop tables (commented out to preserve data)\n    /*\n    await client.dropTable('employees', { ifExists: true, cascade: true });\n    await client.dropTable('departments', { ifExists: true, cascade: true });\n    console.log('✅ Tables dropped');\n    */\n\n    console.log('✅ Cleanup completed');\n\n  } catch (error) {\n    console.error('❌ Cleanup failed:', error);\n  }\n}\n\n// Run the demo if this file is executed directly\nif (require.main === module) {\n  mainSQLDemo()\n    .then(() => {\n      // Uncomment the next line if you want to clean up after the demo\n      // return cleanupDemo(new SynapCores({ host: 'localhost', port: 8080, apiKey: 'aidb_your_api_key_here' }));\n    })\n    .catch(console.error);\n}\n\nexport {\n  mainSQLDemo,\n  tableManagementDemo,\n  indexManagementDemo,\n  transactionDemo,\n  batchOperationsDemo,\n  advancedSQLDemo,\n  analyticsAndReportingDemo,\n  cleanupDemo\n};"